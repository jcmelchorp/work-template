/* import { AppState } from './app.reducer';
import { WorkState } from '../work/store/work.reducer';

export interface AppState {
  app: AppState;
  works: WorkState;
}
 */
export interface AppState { }
